# Contributing

1. Create a feature branch.
2. Keep API changes backward-compatible when possible.
3. Add or update tests for behavior changes.
4. Run `make lint` and `make test`.
5. Never commit secrets or personal employee data.
6. Use conventional commit messages such as `feat:`, `fix:`, `docs:`, `test:`, `chore:`.
